{
  "name": "rundown-demo",
  "private": true,
  "version": "0.0.1",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "pnpm --filter @rundown/web dev",
    "build": "pnpm -r build",
    "contracts:compile": "pnpm --filter @rundown/contracts compile",
    "contracts:test": "pnpm --filter @rundown/contracts test"
  }
}
